# Local development uses Vite's /api proxy by default.
# For a deployed API, set this to its public API base URL, e.g. https://api.example.com/api
VITE_API_URL=https://api.we-hive.com/api
VITE_RAZORPAY_KEY_ID=replace-with-your-razorpay-key-id
